import { AddCircle } from "../../Assets";

const Nav = () => {
  return (
    <nav className="nav container">
      <button>
        <AddCircle />
      </button>
    </nav>
  );
};

export default Nav;
